# 📦 Smart Inventory System (Secure & Scalable)

**Version:** 1.0.0  
**Tech Stack:** PHP 8.2 | MySQL 8.0 | Tailwind CSS v.4

ระบบ POS ที่จะพัฒนาเพิ่มเข้ามานี้ จะทำหน้าที่เป็นจุดขายสินค้าหน้าร้าน โดยพนักงานขาย (Staff) ฟังก์ชันหลักคือการตัดสต็อกสินค้าทันทีเมื่อมีการขาย, การคำนวณยอดเงินรวม, การรับชำระเงิน, และการออกใบเสร็จรับเงิน

---

## 📑 สารบัญ



---

## 🚀 Key Features (ฟีเจอร์หลัก)

### 1. ระบบจัดการสินค้า (Product Management)

- **ข้อมูลครบถ้วน:** จัดการชื่อสินค้า, รูปภาพ, รายละเอียด, ต้นทุน (Cost Price), และราคาขาย (Selling Price)
- **SKU & Barcode:** รองรับรหัสสินค้า (SKU) ที่ไม่ซ้ำกัน และรองรับการสแกน Barcode/QR Code เพื่อความรวดเร็วในการค้นหาและทำรายการ
- **Categorization:** จัดหมวดหมู่สินค้า (Categories/Tags) เพื่อความเป็นระเบียบและค้นหาง่าย
- **Reorder Point:** กำหนดจุดสั่งซื้อขั้นต่ำของสินค้าแต่ละรายการ เพื่อแจ้งเตือนเมื่อของใกล้หมด

### 2. ระบบจัดการสต็อก (Inventory Management)

- **รับสินค้าเข้า (Stock In):** บันทึกจำนวน, วันที่, และ Supplier ที่รับของมา พร้อมคำนวณต้นทุน
- **เบิกสินค้าออก (Stock Out):** ตัดสต็อกจากการขาย หรือการเบิกไปใช้งานจริง
- **ปรับปรุงสต็อก (Stock Adjustment):** รองรับกรณีสินค้าเสียหาย (Damaged), สูญหาย (Lost), หรือการปรับยอดจากการนับสต็อกจริง (Audit)
- **History Logs:** บันทึกประวัติการทำรายการทุกขั้นตอน (Who, When, What) ตรวจสอบย้อนหลังได้

### 3. ระบบบริหารจัดการคู่ค้า (Supplier Management)

- **Supplier Database:** เก็บรายชื่อและข้อมูลติดต่อของผู้ผลิตหรือร้านค้าส่ง
- **Purchase History:** ดูประวัติการสั่งซื้อย้อนหลังได้ว่าเคยซื้อสินค้าชิ้นไหน จากใคร และราคาเท่าไหร่

### 4. ระบบแจ้งเตือนและรายงาน (Alerts & Analytics)

- **Low Stock Alert:** แจ้งเตือนทันทีเมื่อสินค้าลดลงถึงจุด Reorder Point
- **Expire Date Alert:** แจ้งเตือนล่วงหน้าสำหรับสินค้าที่มีวันหมดอายุ
- **Dashboard:** หน้าภาพรวมสรุปยอดคงเหลือ, มูลค่าสต็อกรวม, และสินค้าขายดี (Best Seller)
- **Movement Report:** รายงานความเคลื่อนไหวของสินค้า เพื่อวิเคราะห์แนวโน้มและวางแผนการสั่งซื้อ
- **Thai Language Interface:** ทุกหน้าจอและข้อความในระบบ (เมนู, ปุ่ม, แบบฟอร์ม, การแจ้งเตือน) จะแสดงผลเป็น **ภาษาไทย 100%** เพื่อความสะดวกในการใช้งานของคนไทย

  #ขั้นตอนการทำงานอย่างละเอียด (Detailed Workflow)

### ขั้นตอนที่ 5: การเตรียมขาย (Pre-Sale)

1.  พนักงานเข้าสู่ระบบ และไปที่เมนู "POS" หรือ "ขายหน้าร้าน"
2.  ระบบโหลดหน้า `pos.php` ซึ่งเคอร์เซอร์จะโฟกัสที่ช่อง "ค้นหา/บาร์โค้ด" พร้อมขายทันที

### ขั้นตอนที่ 6: การเพิ่มสินค้าลงตะกร้า (Add to Cart)

1.  **กรณีใช้เครื่องยิงบาร์โค้ด:** พนักงานยิงบาร์โค้ดที่ตัวสินค้า -> ระบบค้นหา `sku` หรือ `barcode` -> ถ้าเจอสินค้า ระบบจะเพิ่มลงในตารางรายการขายด้านขวาทันที (ถ้ามีอยู่แล้ว ให้เพิ่มจำนวน +1)
2.  **กรณีค้นหาด้วยชื่อ:** พนักงานพิมพ์ชื่อสินค้า -> ระบบแสดง Dropdown รายการสินค้าที่ใกล้เคียง -> พนักงานเลือกสินค้า -> เพิ่มลงตาราง
3.  **การปรับจำนวน:** พนักงานสามารถกดปุ่ม +/- หรือพิมพ์ตัวเลขแก้จำนวนสินค้าในตารางได้
4.  **การลบรายการ:** มีปุ่มกากบาท (X) ท้ายแถวเพื่อลบสินค้าที่ไม่อาว

### ขั้นตอนที่ 7: การชำระเงิน (Checkout)

1.  ระบบคำนวณ "ยอดรวมสุทธิ" (Grand Total) ตลอดเวลาที่มีการเปลี่ยนแปลงรายการ
2.  พนักงานกดปุ่ม "ชำระเงิน" (หรือกด Hotkey เช่น F2)
3.  Modal เด้งขึ้นมาให้เลือกวิธีชำระเงิน (เงินสด/โอน) และใส่จำนวนเงินที่รับมา
4.  ระบบคำนวณ "เงินทอน" ให้เห็นทันที
5.  พนักงานกดยืนยัน "บันทึกรายการ"

### ขั้นตอนที่ 8: การบันทึกและพิมพ์ใบเสร็จ (Save & Print)

1.  Frontend ส่งข้อมูลไปที่ `api/pos_checkout.php`
2.  Backend ทำการตัดสต็อกและบันทึกข้อมูลลงฐานข้อมูล (ตามที่ออกแบบในข้อ 2)
3.  เมื่อบันทึกสำเร็จ ระบบจะส่ง `sale_id` กลับมา
4.  Frontend เปิดหน้าต่างใหม่ (Popup) เพื่อแสดงใบเสร็จ (`templates/receipt.php?id=...`) และสั่งพิมพ์อัตโนมัติ (`window.print()`)
5.  หน้าจอ POS เคลียร์รายการทั้งหมด เพื่อเตรียมพร้อมสำหรับการขายบิลถัดไป

---

## 5. ส่วนเพิ่มเติม: ระบบกะและใบเสร็จ (Shift & Receipt System)

### 5.1 ระบบกะ (Shift Management)

ระบบต้องรองรับการทำงานเป็นกะ เพื่อควบคุมเงินสดในลิ้นชัก (Cash Drawer)


**Flow การทำงาน:**

1. **เปิดกะ (Open Shift):** ก่อนเข้าหน้า POS พนักงานต้องระบุ "เงินทอนเริ่มต้น"
2. **ระหว่างกะ:** ขายของตามปกติ
3. **ปิดกะ (Close Shift):** เมื่อเลิกงาน กดปิดกะ -> ระบบสรุปยอดขาย (เงินสด, QR) -> พนักงานนับเงินสดจริงใส่ระบบ -> บันทึกยอด

### 5.2 ระบบใบเสร็จ (Thermal Receipt)

- รองรับเครื่องพิมพ์ความร้อนขนาด **58mm** และ **80mm**
- ใช้ CSS `@media print` จัดรูปแบบให้พอดีกับกระดาษม้วน
- ข้อมูลในใบเสร็จ: ชื่อร้าน, เลขที่ใบเสร็จ, วันที่, รายการสินค้า, ยอดรวม, QR Code (ท้ายใบเสร็จ ถ้ามี)

### 5.3 ระบบ PromptPay QR

- ใช้ Library `promptpay-qr` ในการเจน QR Code จากยอดเงินสุทธิ
- แสดง QR Code บนหน้าจอ POS เมื่อเลือกชำระด้วย "QR PromptPay"
- ระบบต้องตรวจสอบได้ว่าจ่ายแล้ว (ในเฟสแรกอาจจะเป็นการตรวจสอบด้วยตาเปล่า หรือแนบสลิป)


1.  **Authentication (การยืนยันตัวตน):**
    - รหัสผ่านถูกเข้ารหัสด้วย **Argon2id** (Algorithm ที่ปลอดภัยที่สุดใน PHP 8.2 ปัจจุบัน)
    - ป้องกัน **Brute Force Attack** โดยการจำกัดจำนวนครั้งการ Login ผิด
2.  **Session Security:**
    - ป้องกัน **Session Hijacking** ด้วยการ Regenerate ID เมื่อ Login สำเร็จ
    - Cookie ตั้งค่าเป็น `HttpOnly` (JS อ่านไม่ได้) และ `Secure` (HTTPS เท่านั้น)
    - **Auto Timeout:** ระบบจะดีดผู้ใช้ออกอัตโนมัติหากไม่มีการเคลื่อนไหวเกิน 30 นาที
3.  **Data Protection:**
    - **SQL Injection:** ใช้ **PDO Prepared Statements** 100% ในทุกคำสั่ง SQL
    - **XSS (Cross-Site Scripting):** ข้อมูลขาออก (Output) ทุกจุดผ่านฟังก์ชัน `htmlspecialchars()`
    - **CSRF Protection:** ทุกฟอร์ม HTML จะมีการฝัง Token ลับ เพื่อป้องกันการปลอมแปลงคำสั่ง

---




```

---


---

## 📂 Project Structure

โครงสร้างไฟล์แบบ MVC (Model-View-Controller) อย่างง่าย เพื่อความเข้าใจและการดูแลรักษา

```
/Inventory Management System
├── public/                 # Web Root
│   ├── index.php           # Entry Point (Router)
│   ├── assets/             # CSS, JS, Images
│   └── uploads/            # Product Images
├── src/
│   ├── Config/             # Database Connection
│   ├── Controllers/        # Logic การทำงาน
│   ├── Models/             # Class ติดต่อฐานข้อมูล
│   ├── Middleware/         # Auth Check, CSRF
│   └── Helpers/            # Utility Functions
├── templates/              # HTML Views
│   ├── layouts/            # Header, Footer, Sidebar
│   ├── auth/               # Login Page
│   ├── dashboard/          # Dashboard
│   ├── products/           # Product Management
│   └── stock/              # Stock In/Out
├── sql/
│   └── schema.sql          # Database Structure
├── .env.example            # Environment Variables Template
└── README.md               # Documentation
```

---

## 🛠️ Installation & Setup

1.  **Clone Project:**
    นำไฟล์ไปวางในโฟลเดอร์ `htdocs` ของ XAMPP
2.  **Database Setup:**
    - สร้างฐานข้อมูลชื่อ `smart_inventory` ใน phpMyAdmin
    - Import ไฟล์ `sql/schema.sql`
3.  **Configuration:**
    - เปลี่ยนชื่อไฟล์ `.env.example` เป็น `.env`
    - ตั้งค่า DB_HOST, DB_NAME, DB_USER, DB_PASS
4.  **Run:**
    - เปิด Browser ไปที่ `http://localhost/Inventory Management System/public/`
    - Login ด้วย Default Admin (จะระบุใน database)
